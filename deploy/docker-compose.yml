services:
  cortex-mcp:
    build: .
    container_name: cortex-mcp
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"